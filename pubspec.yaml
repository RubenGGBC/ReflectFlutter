name: untitled3
description: Una app de reflexión zen y momentos interactivos
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # DEPENDENCIAS NECESARIAS
  provider: ^6.1.1
  logger: ^2.0.2+1
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0+4
  path: ^1.8.3
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  json_annotation: ^4.8.1
  crypto: ^3.0.3
  get_it: ^7.6.4
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  # Removed: untitled3: any  <-- THIS LINE IS THE PROBLEM

flutter:
  uses-material-design: true